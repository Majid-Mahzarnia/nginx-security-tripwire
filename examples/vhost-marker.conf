server {
    listen 443 ssl;
    server_name example.com;

    # Existing configuration...

    # Put this marker inside the HTTPS server block:
    # NGINX-SECURITY-TRIPWIRE-INSTALL-HERE

    # Existing application routes...
}
